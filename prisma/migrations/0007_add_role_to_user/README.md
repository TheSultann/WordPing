# Migration 0007: Add Role To User

This migration adds role support to the user model.

Keep this directory in sync with the Prisma schema and generated client expectations when changing role-related behavior.
